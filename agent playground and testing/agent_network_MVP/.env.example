# === ADK Agent Team - Example .env ===
# Copy this file to `.env` and fill in values. Never commit your real `.env` to git.

# --- API Keys (required/optional) ---
# Get a key from Google AI Studio: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY

# Optional: enable multi-model via LiteLLM (only if you plan to use these providers)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# --- Platform toggle ---
# If you use direct Gemini API keys (not Vertex AI), keep this False
GOOGLE_GENAI_USE_VERTEXAI=False

# --- Models (override defaults if you want) ---
# Sensible defaults are set in code; uncomment to override.
# MODEL_DIRECTOR=gemini-2.5-pro
# MODEL_PM_DEFAULT=gemini-2.5-pro
# MODEL_WORKER_DEFAULT=gemini-2.5-pro

# --- Security / Guardrail knobs ---
# Avoid logging raw PII in your own logs. Leave False in most cases.
LOG_PII=False

# Cap output length per turn
MAX_TOKENS_PER_TURN=2048

# Hard limit for a single tool execution (seconds)
MAX_TOOL_RUN_SECONDS=15

# Prevent runaway delegation chains
MAX_CHAIN_DEPTH=3

# --- App names (optional, used for namespacing sessions) ---
# APP_NAME=adk_team_secure_base
# USER_ID=demo_user
# SESSION_ID=session_001
